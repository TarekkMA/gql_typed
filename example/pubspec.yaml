name: example
description: A simple command-line application.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.8.1 <3.0.0"

dependencies:
  json_annotation: ^3.0.1
  equatable:
  ferry: ^0.4.1
  gql_dio_link:
  dio:

dependency_overrides:
  code_builder:
    git:
      url: https://github.com/TarekkMA/code_builder
      ref: add-enum

dev_dependencies:
  build_runner:
  pedantic: ^1.9.0
  json_serializable:
  gql_typed:
    #git: https://github.com/TarekkMA/gql_typed
    path: ../
