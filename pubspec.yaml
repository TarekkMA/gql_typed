name: gql_typed
description: Strongly Typed GraphQL.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.8.1 <3.0.0"

#dependencies:
#  path: ^1.7.0

dev_dependencies:
  pedantic: ^1.9.0
#   build_runner: ^1.10.0
#   freezed_annotation: ^0.11.0
#   union: ^0.0.3+1

dependencies:
  code_builder:
    git:
      url: https://github.com/TarekkMA/code_builder
      ref: add-enum
  strings: ^0.1.2
  build: ^1.3.0

  gql_build: ^0.0.11

dependency_overrides:
  code_builder:
    git:
      url: https://github.com/TarekkMA/code_builder
      ref: add-enum
